# Fun Center
موقع تجريبي لمركز ترفيهي، RTL، متجاوب، عروض، حجز، ولوحة إدارة.

## التشغيل
افتح `index.html`.

## قاعدة البيانات
نفّذ `supabase-schema.sql` داخل مشروع Supabase. لا تضع Service Role Key داخل الواجهة الأمامية.
